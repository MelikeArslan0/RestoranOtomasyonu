@model IEnumerable<RestoranOtomasyonu.Models.kategori>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/YönetimPaneli/Views/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a class="btn btn-success" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KategoriAd)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KategoriAd)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.KategoriID">Edit</a> |
                    <a asp-action="Details" class="btn btn-warning" asp-route-id="@item.KategoriID">Details</a> |
                    <a asp-action="Delete" class="btn btn-warning" asp-route-id="@item.KategoriID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
